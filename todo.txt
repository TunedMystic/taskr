~ Review permissions.
~ Review viewsets.


import datetime; import json; from django.contrib.auth.models import User; from rest_framework.renderers import JSONRenderer; from meeting.models import Meeting; from meeting.serializers import MeetingSerializer, UserSerializer; m = lambda: Meeting.objects.all(); u = lambda: User.objects.all(); ms = lambda: MeetingSerializer.objects.all(); r = lambda x: JSONRenderer().render(x.data); data = {"person": "ironman", "date": datetime.datetime.now(), "location": "california", "priority": 1, "company": "stark industries", "notes": "he's ironman!!"}; ms1 = MeetingSerializer(m()[len(m()) - 1]); ms1d = JSONRenderer().render(ms1.data); group = m()[:5]; groupserializer = MeetingSerializer(group, many = True); groupjson = JSONRenderer().render(groupserializer.data); u1 = u()[0]; u1s = UserSerializer(u1); data["user"] = u1.pk; m1 = MeetingSerializer(data = data); m1.is_valid();